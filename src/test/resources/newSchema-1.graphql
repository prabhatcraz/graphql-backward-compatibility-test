type Person {
    name: String
    age: Int
    address: Address
    educations: [Education]
}

type Address {
    street: String
}

type Education {
    school: School
    graduated: Boolean
    completed: [Int]
}

type School {
    name: String!
    address: Address
}
